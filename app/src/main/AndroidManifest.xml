<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gerardoleonelbleslylontaan.akb_mobile">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/akb_logo_full"
        android:label="@string/app_name"
        android:roundIcon="@drawable/akb_logo_full"
        android:supportsRtl="true"
        android:theme="@style/Theme.Akb_mobile"
        android:usesCleartextTraffic="true">
        <activity android:name=".pesanan.FinalCartActivity"/>
        <activity android:name=".pesanan.OrderActivity" />
        <activity android:name=".menu.DetailActivity" />
        <activity android:name=".pesanan.DetailPesanActivity" />
        <activity android:name=".menu.QrCodeActivity"></activity>
        <activity android:name=".pesanan.PesananActivity"/>

        <activity android:name=".menu.MenuActivity"/>
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="com.google.zxing.client.android.SCAN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>